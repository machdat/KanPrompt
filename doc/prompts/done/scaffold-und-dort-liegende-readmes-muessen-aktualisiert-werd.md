# Scaffold und dort liegende readmes müssen aktualisiert werden

## Status: Obsolet — durch andere Tickets abgedeckt

Dieses Ticket wurde geschlossen ohne Umsetzung. Die beschriebenen Anforderungen wurden vollständig durch folgende Tickets abgedeckt:

- **schema-versioning** — Schema-Versionierung eingeführt, `.kanprompt-version.json` pro Projekt, `workflow/schema.json` als Quelle der Wahrheit
- **one-step-deploy** — `update.ps1` mit `git pull` und Versionsvergleich
- **update-workflow-docs-timestamps** — verbleibende Doku-Aktualisierungen (noch offen)

---

# Session-Log

- **Datum:** 2026-03-15
- **Branch:** master
- **Ergebnis:** Obsolet — ohne Umsetzung geschlossen

## Zusammenfassung

Ticket wurde als obsolet erkannt und geschlossen. Die Kernanforderungen (Versionierung, Update-Script, Scaffold im Repo) wurden durch die Tickets `schema-versioning` und `one-step-deploy` umgesetzt. Verbleibende Doku-Updates werden in `update-workflow-docs-timestamps` behandelt.

## Geänderte Dateien

Keine.

## Abweichungen vom Prompt

Nicht umgesetzt — durch andere Tickets ersetzt.

## Offene Punkte

Keine.
